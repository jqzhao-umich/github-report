# Core dependencies. Kept in sync with pyproject.toml so `pip install -r
# requirements.txt` and `uv sync --frozen` install the same set. For
# reproducible installs (Docker + CI), use uv.lock via `uv sync --frozen`;
# this file exists for developers who prefer plain pip.
fastapi>=0.68.1
uvicorn>=0.15.0
PyGithub>=2.3.0
python-dotenv>=0.19.0
requests>=2.26.0
mcp>=2.0
httpx>=0.27.0
pydantic>=1.10.12
pydantic-settings>=2.0
PyYAML>=6.0
Markdown>=3.3.4
APScheduler>=3.10.0
GitPython>=3.1.0
azure-devops>=7.1.0b4
pytz>=2021.1

# Testing dependencies. Pins updated for Python 3.14 compatibility;
# lower bounds match pytest-asyncio 1.x contract (auto/opt-in modes only,
# @pytest.mark.asyncio required on each async test).
pytest>=8.3
pytest-asyncio>=0.24,<2
pytest-cov>=5.0
pytest-env>=1.1
pytest-mock>=3.14
pytest-benchmark>=4.0
psutil>=5.9
aioresponses>=0.7.6
responses>=0.25
